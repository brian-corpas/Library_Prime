<div class="card">
  <div class="card-body">
    <ul>
       <% @books.each do |book| %>
         <li><%= book.title %></li>
         <li><%= book.author %></li>
       <% end %>
    </ul>
  </div>
</div>
